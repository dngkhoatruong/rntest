import { SIGN_IN_SCREEN } from '../../constants/screenKeys';
import SignIn from '../../screens/SignIn';

const AuthRoutes = [
  {
    key: SIGN_IN_SCREEN,
    name: SIGN_IN_SCREEN,
    compoent: SignIn,
  },
];

export default AuthRoutes;
